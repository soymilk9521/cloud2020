server:
  port: 9002

spring:
  application:
    name: cloud-nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.80.71:8848

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"